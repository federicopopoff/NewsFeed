@if (Request.IsAuthenticated)
{

    <b> Welcome </b> @HttpContext.Current.User.Identity.Name <text> </text> @Html.ActionLink("Log Out", "LogOff", "Account")

}
else
{
    @Html.ActionLink("Log In", "LogOn", "Account")

}
